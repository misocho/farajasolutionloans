# Faraja Solution Loans — Backend dependencies
# NOTE: pyproject.toml + uv.lock are the source of truth (uv-managed project).
# This file mirrors pyproject.toml for non-uv workflows (e.g. Docker/pip).

# ── Runtime ───────────────────────────────────────────────────────────────────
alembic>=1.18.5
apscheduler>=3.11.3
email-validator>=2.3.0
fastapi>=0.139.2
fastapi-users[sqlalchemy]>=15.0.5
psycopg[binary]>=3.3.4
pwdlib>=0.3.0
pydantic-settings>=2.14.2
python-dotenv>=1.2.2
python-jose[cryptography]>=3.5.0
python-multipart>=0.0.32
reportlab>=5.0.0
resend>=2.35.0
sqlalchemy>=2.0.51
structlog>=26.1.0
uvicorn[standard]>=0.51.0

# ── Dev / tooling ─────────────────────────────────────────────────────────────
httpx>=0.28.1
mypy>=2.3.0
pytest>=9.1.1
pytest-asyncio>=1.4.0
ruff>=0.15.22
types-passlib>=1.7.7.20260211
types-python-jose>=3.5.0.20260408
