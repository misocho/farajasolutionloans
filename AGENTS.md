# AGENTS.md — Faraja Solution Loans

> Instructions for AI coding agents working in this repo. This file is behavior-shaping, not documentation: read it, follow it, keep it up to date.

## Role & working style

Act as a **senior staff engineer** on a production microfinance (lending) system.

- **Inspect before assuming.** Never guess how something works — read the actual files first (`PLAN.md`, the relevant router/model/page, `AGENTS.md`).
- **Propose before changing.** For anything non-trivial, state the plan and the smallest change that achieves it before writing code.
- **Keep changes minimal.** Never rewrite working code, restructure files, or "improve" style unless asked.
- **Three-strikes rule.** If the same fix fails three times, stop, re-read the surrounding code, and report the blocker instead of retrying.
- **Never fabricate.** No invented test results, no fake data in new code, no pretending an endpoint exists. If something doesn't exist, say so.
- **No tests.** This project has **no test suite** — do not create test files, fixtures, or test tooling unless explicitly asked.
- **Money is real.** This is a lending system: a rounding or duplicate-payment bug costs actual money. Slow down on anything touching amounts, statuses, or dates.
- **Report honestly.** After a task, state exactly what changed and what you verified (commands run, build/lint results).

## Roadmap execution (per-item workflow)

We work through the **Roadmap to Launch** checklist in `PLAN.md`. Every completed item follows the same loop:

1. Implement the smallest safe change (verify with `ruff check` backend / `pnpm build` + `pnpm lint` frontend).
2. Tick the item `[x]` in `PLAN.md`; if the item changes architecture, permissions, or known bugs, update the matching section here and in `PLAN.md` too.
3. Commit code + `PLAN.md` + `AGENTS.md` together in **one logical commit** (Conventional Commits: `feat:`, `fix:`, `docs:`).
4. Push immediately (`git push origin main` → Render auto-deploy).
5. Confirm the item is live on staging before moving to the next.

- One commit per item; no force push, no amending pushed commits.
- Deferred items get `[~]` plus a note in the Decisions Log (`PLAN.md`) and are never silently dropped.

## Project overview

Microloan management system for Faraja Solution Loans (Kenya): loan products (Faraja 4wk / 5wk / Lumpsum), client registration with KYC documents and signatures, loan workflow (pending → approved → disbursed → closed), weekly installment tracking, penalty calculation (3% per 2 days), repayments with manual verification, branches, users/roles/permissions, reports, and notifications. Master plan + live progress: `PLAN.md` (read before starting work).

## Tech stack

| Layer | Technology |
|---|---|
| Backend | Python >=3.14 · FastAPI · SQLAlchemy 2.0 · Alembic · PostgreSQL (psycopg3) · python-jose · Resend · ReportLab (unused) · APScheduler (unused) |
| Frontend | Next.js 16.2 (Turbopack) · React 19 · TypeScript · Tailwind 4 · shadcn-style UI · TanStack Query/Table · axios · react-hook-form + zod · recharts · react-signature-canvas · sonner |
| Packaging | Backend: `uv` · Frontend: `pnpm` — **never npm** (deployment build runs pnpm) |

## Commands (prove-it)

```bash
# Backend (backend/)
uv run uvicorn app.main:app --reload      # dev server :8000 (real entry is app/main.py)
uv run alembic upgrade head
uv run ruff check . && uv run ruff format .   # line-length 100, double quotes
uv run mypy app                           # strict type checking

# Frontend (frontend/)
pnpm dev                                  # dev server :3000
pnpm build                                # MUST pass before finishing frontend work (Vercel runs this)
pnpm lint                                 # eslint
```

Before finishing any frontend task: run `pnpm build` and `pnpm lint`. Backend: `ruff check`. Fix only issues your change introduced — the repo has pre-existing lint errors (84) and no tests; leave unrelated ones alone unless asked.

## Agent tooling — skills & MCPs (installed 2026-08-07)

Use these when relevant; they are the sanctioned way to fill framework-knowledge gaps and verify UI work.

- **Skills** (`.opencode/skills/frameworks/`, load on demand at the start of relevant work):
  - `fastapi` — backend conventions: Pydantic v2 schemas, versioned routes, dependency injection, structured logging. Load before backend work in `backend/`.
  - `nextjs` — frontend conventions: App Router, shadcn/ui primitives, server components, Tailwind tokens, accessibility. Load before frontend work in `frontend/`.
- **MCPs** (context7 is global in `~/.config/opencode/opencode.jsonc`; playwright is repo-local in `opencode.json`):
  - `context7` — live library docs (FastAPI, Pydantic, SQLAlchemy, Next.js, React). Consult it instead of guessing version-specific APIs — especially for Next.js 16 breaking changes (complements `frontend/node_modules/next/dist/docs/`).
  - `playwright` — browser verification of frontend flows. Use for end-to-end checks of UI work (it is the exception to the frontend "no browser" rule, updated 2026-08-07). The human mobile-width (375px) check remains mandatory.

## Kenya locale & financial rules (non-negotiable)

- **Currency:** Kenyan Shilling (KES). Display with `Intl.NumberFormat("en-KE", { style: "currency", currency: "KES", maximumFractionDigits: 0 })` — never hardcode "KES" strings and never format amounts with bare `.toLocaleString()` without the `en-KE` locale. Dates: `toLocaleDateString("en-KE", ...)`, timezone `Africa/Nairobi` (`settings.DEFAULT_TIMEZONE`).
- **Money = Decimal, never float.** Use `Decimal` in Python and `Numeric(18, 2)` in DB columns (existing models already do this — keep it). Round only at display/persist boundaries, as late as possible. Never sum floats.
- **Don't invent money.** Partial payments must not round up or drop residuals; track exact amounts.
- **Idempotency & duplicate protection.** Any new money-moving endpoint (repayments, fees, future M-Pesa) must be safe against double submission — unique constraints on reference numbers, check-before-insert, or an idempotency key. The system is still manual-verified: repayment recording and verification are separate steps (four-eyes principle, `recorded_by` vs `verified_by`).
- **Audit trail.** Financial state changes (loan approve/disburse/close, repayment record/verify) must record who did it (`*_by_id` fields) and when. Never add a column like "updated" to fake an audit trail; append events.
- **KYC & data protection (Kenya Data Protection Act 2019, ODPC).** Client data includes national ID numbers, KRA PINs, photos, and signatures — collect only what the form requires, never log them, never expose them in list endpoints, treat consent as a real requirement. Photos/signatures may be base64 in dev; S3 in prod.
- **Payment modes:** `Cash`, `MPesa`, `BankTransfer`, `Cheque`, `Other` (existing enum — don't invent new modes). Kenyan phone numbers: store as `+2547XXXXXXXX`-compatible format.
- **Swahili/locale terms used in the product:** "Kesho kutwa" = day after tomorrow (used for due-date notifications). Keep them where they exist.
- **Compliance-aware reporting:** reports/arrears must support CBK-style classification (days past due, non-performing exposure) — don't silently drop overdue or past-maturity data from aggregations.

## Code style

**Backend (FastAPI)**
- Routers = endpoints only; business logic goes in `app/services/` (see `loan_service.py`).
- Auth on every endpoint: `Depends(get_current_user)` + `_require_permission(user, "perm.name")` — permission names MUST exist in `app/core/permissions.py` (a typo silently 403s everyone).
- Models: SQLAlchemy 2.0 style — `Mapped[...]` + `mapped_column(...)`, UUID PKs, `BaseModel` in `app/models/base.py`. New tables need an Alembic migration.
- Enums from `app/models/enums.py` (StrEnum). Serialize with `.value`.
- Format: ruff, line-length 100, double quotes, section-divider comments like existing files.

**Frontend (Next.js 16)**
- ⚠️ Next.js 16 has breaking changes vs older versions — read `frontend/node_modules/next/dist/docs/` before writing code (see `frontend/AGENTS.md`).
- Functional components only; `"use client"` for pages (this app is fully client-side, no `route.ts`).
- All HTTP via `features/*/api.ts` modules using the axios instance from `app/lib/api.ts` — the canonical loan/client/repayment/report module is `features/clients/api.ts`. Add typed functions there; never call axios directly from a page.
- Data fetching via TanStack Query (`useQuery`/`useMutation`); validation with zod; forms with react-hook-form.
- Never use `any`; keep functions small; use `async/await`; no hardcoded fallback numbers (existing dashboard/settings pages violate this — don't extend the pattern).
- UI primitives from `components/ui/` (shadcn-style). Brand colors: primary `#0D44A2`, accent `#F57424`. Tailwind 4 (no config file).

## Architecture (where things live)

- Backend entry: `app/main.py` → `app/create_app.py`; routers registered in `app/api/router.py` under `/api/v1`. `backend/main.py` re-exports `app.main.app`.
- Routers: `auth`, `admin`, `loans_clients` (clients, loans, installments, repayments, dashboard stats), `branches`, `reports`, `notifications`, `search`, `fees`, `seed`, `expenses` (added 2026-08-17), `audit` (added 2026-08-17), `health` (added 2026-08-17, `GET /api/v1/health`).
- **Audit trail (E2, 2026-08-17):** `audit_logs` table (`app/models/audit_log.py`, migration `0482b1f8e6ef`) — actor_id/actor_name, action (`entity.action` dotted string), entity, entity_id (str), branch_id, JSONB `meta`. Writes via `write_audit_log(db, actor_id, actor_name, action, entity, entity_id, branch_id, meta)` in `app/services/audit_service.py`, called before `db.commit()` on: client.create, loan.create, loan.approve/reject/disburse/close, loan.note (meta note), loan.status_override (meta status_override), repayment.record (flush first; meta amount+mode), repayment.verify (meta amount), fee.record/verify (meta amount+mode), expense.record/verify (meta amount+category). `GET /audit-logs` (audit.py, `audit.view`, filters action/entity/branch_id/date_from/date_to, limit≤500) is branch-scoped for non-`branches.view_all` users. Frontend: `/audit` page (filters + table/mobile cards) + sidebar "Audit Logs" item gated on `user.permissions.includes("audit.view")` (permission-gated, NOT role-name).
- **Client KYC PDF (E1, 2026-08-17):** `app/services/pdf_service.py` (ReportLab) builds a branded A4 KYC record — personal/spouse/dependants/business/next-of-kin/guarantor/properties sections, active-loans table (uses DB columns only: `status`, `interest_amount`, `disbursed_date`, `total_repayable` — `db_status`/`outstanding_balance` are read-time computed, NOT model columns), base64 photos+signatures embedded when present. `GET /clients/{id}/pdf` (loans_clients.py) gated `clients.view` + `_assert_branch_visible`. Client drawer "PDF" button via `fetchClientPdfApi` (blob download).
- **APScheduler daily jobs (D2, 2026-08-17):** `app/tasks/scheduler.py` (`start_scheduler`/`stop_scheduler`, module-level singleton, started in `create_app` lifespan) runs `app/tasks/daily_jobs.py` at 08:00/08:05 Africa/Nairobi: (1) `daily_digest_job` emails ONE due/arrears digest per active user holding `loans.view` — sections T-2/due-tomorrow/due-today/arrears-past-maturity, branch-scoped via `get_user_branch_ids`, each section suppressed per user's `notification_prefs`; (2) `penalty_snapshot_job` upserts `penalty_snapshots` (loan_id+snapshot_date unique, migration `3275d243045a`) — outstanding/penalty (product-capped)/days_overdue for overdue disbursed loans. Loan approved/disbursed → submitting-LO emails fire inline in the approve/disburse endpoints (`email_service.send_loan_approved_email`/`send_loan_disbursed_email`, failures swallowed). Beware detached-instance pitfalls: keep per-user permission queries on the SAME session that loaded the users.
- Branch scope helper: `get_user_branch_ids(db, user)` in `app/core/permissions.py` (None = unrestricted; list = scoped; empty = see nothing). Used by clients/loans/repayments/search/branches/dashboard-stats/expenses/notifications. **Permission-driven since 2026-08-17:** a user holding `branches.view_all` (Director, System Admin, Auditor) is unrestricted; everyone else is scoped to their `user_branch` rows — there is NO role-name check (the old `UNRESTRICTED_ROLES = {Director, System Admin, Auditor}` set is gone; do not reintroduce role-name logic). Manager's role list **excludes** `branches.view_all` (Managers stay branch-scoped by design). **Write side enforced too (2026-08-07):** client create/update and loan create validate the branch — scoped users can only assign their own branch (403), unrestricted users must supply an existing branch (400); `GET /clients/{id}` is branch-scoped like the list; client registration UI sends `branch_id` (dropdown for unrestricted, auto-assigned for scoped) and gates on the real `clients.create` permission. **Top-nav dropdown (2026-08-17):** the `BranchSelector` lists ALL active branches when `me.permissions` includes `branches.view_all`, else filters by `me.branch_ids` — the frontend must mirror the permission, never the role names (reports page `isScoped = !permissions.includes("branches.view_all")` is the reference). Loan Officer and Finance Officer roles gained `branches.view` (2026-08-17, additive reseed) so their dropdowns can load their branch.
- **Loan list/detail branch scoping (fixed 2026-08-17):** `GET /loans?branch_id=` now goes through `_resolve_branch_filter` (scoped users get 403 on out-of-scope branch filters; before, the explicit param silently bypassed scope — any `loans.view` holder could read any branch's loans) and `GET /loans/{id}` checks `_assert_branch_visible` (was completely unscoped). Clients/repayments/dashboard/branches were already scoped. **Write side too (2026-08-17):** every loan action endpoint (`PATCH /loans/{id}` approve/reject/disburse/close/notes/status) calls `_assert_branch_visible(db, current_user, loan.branch_id)` after loading the loan — before, a scoped Manager could approve/disburse/close/annotate loans in any branch. Repayments/fees got the same treatment: `POST /repayments` + `PATCH /repayments/{id}/verify` check the loan's branch; `POST /fees` + `POST /fees/{fee_id}/verify` + `GET /fees/quote` check the client's branch and `GET /fees` is branch-filtered via a Client join; `POST /expenses/{id}/verify` checks the expense branch (the `_assert_branch_visible` helper lives per-router in `loans_clients.py`, `fees.py`, `expenses.py`).
- **Client list slims media (2026-08-07):** `GET /clients` returns base64 photo/signature fields as `null` (payload bloat); the full KYC media comes only from `GET /clients/{id}` via `fetchClientApi` — the client detail drawer fetches per client (`["client-detail", id]` query, snapshot → live swap, Retry banner on error). Don't re-add media to the list serializer.
- **Maps links (2026-08-11, C7):** the client detail drawer shows clickable "Open in Google Maps" rows for `residential_maps_link` / `business_maps_link` when set. The registration-form maps inputs + live preview were **removed** the same day (owner decision) — do not re-add form inputs for these; the backend fields remain accepted/serialized (`features/clients/api.ts` `Client` type carries them).
- **Client drawer loans (2026-08-09):** the client detail drawer lists the client's loans (`fetchClientLoansApi` → `["client-loans", id]`, `GET /loans?client_id=` returns `product`, `outstanding_balance`, computed status fields). Tapping a loan inline-expands `LoanInstallmentPanel` which fetches `["loan", id]` and renders `components/loans/installment-timeline.tsx` (shared with the loans drawer). Status rendering is canonical: `components/ui/status-badge.tsx` — never hand-roll per-page `STATUS_CFG` maps (loans page migrated 2026-08-09).
- **Profile photos (2026-08-07):** `GET /auth/me` returns `profile_photo` (base64, `AuthUser` schema ← `users.profile_photo` set by the invite `complete-profile` step). Sidebar user card and topbar user-menu render `<img class="size-full rounded-full object-cover">` inside the Avatar with the initials `AvatarFallback` when absent. Keep the fallback whenever a new avatar appears.
- **Dashboard activity feed (2026-08-07):** `/dashboard/stats` `recent_activity` emits 6 types — `repayment`, `loan` (application), `approval`, `disbursement`, `client`, `fee` — each queried per module (latest 8), merged and sorted by time desc, trimmed to 8. Frontend `DashboardActivity` union + `ACTIVITY_ICONS` map must be extended whenever a new event type is added (icon-only Record, missing keys break the build).
- **Notification prefs (2026-08-07):** per-user `notification_prefs` table (user_id PK, JSONB `prefs`, default all-true in `DEFAULT_PREFS` in `app/models/notification_pref.py`). `GET/PATCH /notifications/preferences` (Pydantic `NotificationPrefs` schema — field names are the 6 live notification types: `due_today`, `due_tomorrow`, `almost_due`, `arrears`, `repayment_pending`, `pending_approval`). `_build_notifications` filters server-side by prefs. New notification types MUST be added to `DEFAULT_PREFS` + the `NotificationPrefs` schema + Settings toggle list in `settings/page.tsx` + frontend `NotificationPrefs` type.
- **Settings page honest (2026-08-07):** no fake UI left — password change is live (C2), notification toggles are live (U6), Appearance section removed (was decorative; there is no `.dark` theme toggle in the app), System Info shows true facts (JWT 60 min, PostgreSQL live, storage = DB base64 / S3 in prod). `ACCESS_TOKEN_EXPIRE_MINUTES=60` in `backend/.env` (gitignored — if staging overrides it in the Render env vars, keep it at 60).
- **Repayments partial-payment behavior (2026-08-09):** verified end-to-end on staging. `mark_installments_paid` (`loan_service.py`) marks an installment `Paid` only when cumulative verified repayments fully cover it — partial payments leave it `Pending` (installment `Late`/`Missed` are computed at read time, never stored). Loan `Arrears` is also computed at read time (`get_computed_loan_status`: outstanding > expected given due-to-date payments). The repayments record form shows an amber warning when the entered amount is below the next unpaid installment — don't make the backend store `Late`/`Missed` without a migration + owner approval.
- **Defaulters repay (2026-08-17):** the repayments page loan dropdown filters `l.db_status === "Disbursed"` (DB status), NOT computed `status` — defaulters ("Defaulter"/"Past Maturity"/"Arrears") must stay selectable for repayment recording. Backend `POST /repayments` already only checks DB status. Don't revert to filtering on computed `status` (that bug hid defaulters from the dropdown).
- **Loan notes + manual status override (2026-08-17, Manager-only):** `PATCH /loans/{id}/notes` (`{note}`, permission `loans.update`, any loan state) appends `[dd Mmm yyyy HH:mm — Name]` to `loan.notes` (the notes field is a log — no separate audit columns). `PATCH /loans/{id}/status` (`{status_override}`) sets/clears `loans.status_override` (+ `status_override_by_id`/`status_override_at`, migration `697c72921ff3`) — values limited to `{Defaulter, Past Maturity, Arrears, Performing, Almost Due, Due}` (**"Paid" excluded on purpose** — fully repaid loans derive "Paid" automatically and an override could hide outstanding debt), only on disbursed loans, 400 otherwise. `get_computed_loan_status` returns the override for disbursed loans **except** when `outstanding <= 0` ("Paid" always wins). Loan drawer: status select + Add Note box, gated `loans.update` (Manager/Director/System Admin — **Loan Officers do NOT get it**, owner decision), status select only when `db_status === "Disbursed"`. Serialized on every loan payload: `db_status`, `status_override`, `status_override_by`. List/detail queries `joinedload(Loan.status_override_by)`.
- **Deep-link params (2026-08-07, handler fixed 2026-08-17):** dashboard pages consume one-shot URL params and clear them via `history.replaceState`: `/clients?client=<id>` opens the detail drawer, `/loans?loan=<id>` opens the loan drawer, `/loans?apply=true` opens the new-loan form. Topbar global search navigates with the record id (`?client=`/`?loan=`); notification rows do the same. ⚠️ **Implementation: use a one-shot `useEffect` on mount, NOT render-time state init** — React 19 + Next 16 drops the render-phase update during SPA navigation (the param then only gets consumed on the next re-render). Loans page migrated 2026-08-17; the pattern in `loans/page.tsx` (~line 417, `eslint-disable` for `set-state-in-effect`/`exhaustive-deps`) is the reference. Don't use `useSearchParams` on these static pages (Suspense constraint).
- **Notifications page + click-to-navigate (2026-08-17):** new `app/(dashboard)/notifications/page.tsx` (KPI tiles Unread/Total/Critical, All/Unread toggle, Mark all read, 30s refetch, `["notifications"]` query shared with the bell) + sidebar nav item "Notifications" (Bell icon, after Schedule). Bell dropdown rows navigate to the related record via `notificationTarget()` (`app/lib/notifications.ts` — shared `PRIORITY_CFG`/`TYPE_ICON`/`timeAgo` helpers too): `repayment_pending` → `/repayments`, loan-bearing types → `/loans?loan=<id>`. **`GET /notifications` is branch-scoped server-side** (`get_user_branch_ids`, applied per section in `_build_notifications`) so LO/Manager see only their branch. The Finance Officer role gained `loans.view` in `app/db/seed.py` (additive — reseed to apply) so loan deep-links work for it.
- **Loan products admin (2026-08-09):** `GET /admin/loan-products` (ALL products incl. inactive, has `is_active`) + `PATCH /admin/loan-products/{id}` (C6) in `admin.py`, gated `check_is_director` (Director/System Admin — Managers cannot edit, per owner decision). Editable via `LoanProductUpdate` (`app/schemas/loan_products.py`): penalty_rate 0–1, penalty_interval_days ≥1, max_penalty_amount ≥0 or null, is_active. `GET /loan-products` (public list) stays active-only and does NOT include `is_active` — admin list + frontend `LoanProduct` type in `features/admin/api.ts` are separate. Penalty is computed live from the product at assessment (`loans_clients.py:124`) — edits retroactively affect live loans; the Admin Console edit modal (users page "Loan Products" tab) warns the user about this. Don't silently change seeded product rates.
- **Loan product creation (2026-08-17):** `POST /admin/loan-products` in `admin.py`, same `check_is_director` gate, body = `LoanProductCreate` (`app/schemas/loan_products.py`): name 1–150, product_type (enum — now includes `CUSTOM`, added by migration `c04609b1b091` via `ALTER TYPE ... ADD VALUE IF NOT EXISTS 'CUSTOM'`; other migrations may create products without needing enum changes), duration_days ≥1, interest_rate 0–1, penalty_rate default 0.03, penalty_interval_days default 2, max_penalty_amount ≥0 or null, is_active default true. Duplicate name → 409. Frontend: "New Product" button + create modal on the users page "Loan Products" tab (`createLoanProductApi` in `features/admin/api.ts`, `createProductMutation` invalidates both `["admin-products"]` and `["loan-products"]`). New products with a duration not divisible by 7 produce non-weekly installment spacing via `num_weeks = duration_days // 7` (`loan_service.py:165`) — fine for Lumpsum/Custom.
- **Loan quote endpoint (2026-08-17):** `GET /loan-products/{id}/quote?amount=` in `loans_clients.py` (gated `loans.view`, 400 below KES 4,000, 404 unknown product) → `LoanQuoteResponse` via `loan_service.quote_loan` — the exact disbursement math (flat interest, `duration_days // 7` installments, HALF_UP at 2dp) plus both application-fee tiers (`application_fee_new`/`application_fee_existing`). The dashboard Loan Calculator (`components/dashboard/loan-calculator.tsx`) is fully plan-driven: amount + product only, rate/installments derived from the plan (never editable — edits would produce wrong estimates), fee shown with a New/Existing toggle, results via `fetchLoanQuoteApi` (`features/clients/api.ts`).
- **Expense recording module (2026-08-17):** `expenses` table (`app/models/expense.py`, migration `3593c62a6020`): branch_id FK (SET NULL, indexed), `ExpenseCategory` StrEnum (Salaries/Rent/Utilities/Transport/Marketing/Stationery/Operations/Other), amount Numeric(18,2), expense_date Date, mode PaymentMode, reference, description, recorded_by_id, verified/verified_by_id/verified_at. Router `app/api/routers/expenses.py`: `GET /expenses` (expenses.view, branch-scoped via `get_user_branch_ids`), `POST /expenses` (expenses.create, `_resolve_branch_assignment` — unrestricted roles must supply branch_id → 400, scoped roles auto-assigned), `POST /expenses/{id}/verify` (expenses.approve, four-eyes: **recorder cannot verify own expense** → 400; 404 unknown, 400 already verified). No update/delete by design. **Scope caveat:** `get_user_branch_ids` treats `branches.view_all` holders (Director/System Admin/Auditor) as unrestricted even when they have `user_branch` rows — the frontend mirrors this via `isScoped = !permissions.includes("branches.view_all")` (reports page) — do not detect scope from `branch_ids` alone.
- **Profit & Loss report (2026-08-17, D1):** `GET /reports/pnl?month=&year=&branch_id=` + `GET /reports/pnl/series?months=6&branch_id=` in `reports.py` (reports.view). Accrual at disbursement: interest income = Σ `interest_amount` of loans disbursed in the period (status Disbursed/Closed); fee income = **verified** fee payments by `verified_at`; expenses = **verified** expenses by `expense_date`; `collected` = verified repayments in period; penalties = read-time memo (`get_outstanding`+`calculate_penalty`, `with_penalties` flag) — not accrued. Frontend "Profit & Loss" tab (`app/(dashboard)/reports/page.tsx`, 4th tab): month/year/branch filters (branch filter hidden for scoped users), statement table, KPIs, 6-month income-vs-expenses BarChart, client-side CSV export, Expenses card with Record Expense modal + Approve (own records hide the Approve button). Write-offs NOT built (deferred).
- **Performance pass (2026-08-17, owner-approved free-tier mitigations):** `GET /repayments` list returns `receipt_photo: null` (payload 805 KB → ~7 KB on staging); the drawer lazy-fetches it via new `GET /repayments/{id}` (`_serialize_repayment(r, include_media=False)`, gated `repayments.view` + `_assert_branch_visible`, frontend `fetchRepaymentApi` + `["repayment", id]` query). Loans list outstanding is ONE grouped SUM over all loans (repaid_map) passed into `_enrich_loan(loan, db, outstanding=...)` — no per-loan `get_outstanding` calls; calendar `paid_amount` uses one SUM + one installments query with the same walk logic as `installment_paid_amounts`; `_build_notifications` uses one grouped repaid query + `outstanding_of()` helper (was 4 per-loan calls). `GET /health` now runs `SELECT 1` (`db: up/down`). Frontend page queries (`loans`, `clients`, `repayments`, `dashboard-stats`, `installment-calendar`, `loan-products`) gained `staleTime: 60_000`. GitHub Actions `.github/workflows/keep-alive.yml` (cron */5, free) pings `/api/v1/health` to keep Render + Neon warm — no paid upgrade by owner decision.
- `app/services/` = business logic · `app/models/` = schema · `app/core/` = config/security/permissions/seed data · `app/schemas/` = Pydantic.
- `app/tasks/` (empty) = future APScheduler jobs · `app/reports/` (empty) = future report layer · `app/storage/s3_service.py` = dev no-op until prod.
- **Deployment (2026-08-19):** repo is DigitalOcean-ready — `backend/Dockerfile` + `frontend/Dockerfile` (uv / pnpm multi-stage; frontend uses `output: "standalone"`), `docker/nginx.conf` + `docker-compose.yml` `deploy` profile (droplet path; default `docker compose up -d` still starts only Postgres for local dev), `.do/app.yaml` (App Platform path), walkthrough in `docs/DEPLOY.md`. CORS origins are now env-configurable via `CORS_ORIGINS` (comma-separated, default `http://localhost:3000,https://faraja.enkaai.net`) — no longer hardcoded in `create_app.py`. Backend container runs `alembic upgrade head` at boot (idempotent).
- Frontend pages: `app/(dashboard)/{dashboard,loans,clients,repayments,reports,schedule,branches,users,settings}/page.tsx` (single-file pages, detail views are drawers). Auth: `app/(auth)/{login,accept-invite}`. JWT cookie `faraja_token` via `app/lib/auth.ts`; axios interceptor auto-redirects to `/login?expired=true` on 401.

## Canonical permissions (`app/core/permissions.py`)

`dashboard.view` · `audit.view` · `branches.{view,view_all,manage}` · `clients.{view,create,update,delete}` · `loans.{view,create,approve,reject,disburse,update,writeoff}` · `repayments.{view,record,verify}` · `fees.{view,record,verify}` · `reports.{view,export}` · `roles.{view,manage}` · `users.{view,create,update,delete}` · `settings.manage` · `expenses.{view,create,approve}` (live since 2026-08-17, seeded roles already include them). `branches.view_all` (2026-08-17) = unrestricted branch scope; granted to Director/System Admin/Auditor, **explicitly excluded from Manager** (Managers stay branch-scoped).

Use these exact strings. Known historical violations (do not reintroduce): `clients.edit` → `clients.update`; `repayments.create` → `repayments.record`. New `fees.*`/`branches.*` permissions require a seed re-run (`app/db/seed.py` is idempotent) to take effect.

## Business rules quick reference

| Product | Duration | Rate (flat) | Installments | Penalty |
|---|---|---|---|---|
| Faraja 4 Weeks | 28 days | 20% | 4 weekly | 3% every 2 days on outstanding |
| Faraja 5 Weeks | 35 days | 30% | 5 weekly | 3% every 2 days |
| Lumpsum | agreed | TBD (20% placeholder) | lump at end | TBD |

- Installments = equal weekly splits of total repayable, generated at disbursement.
- Loan statuses: `Pending / Approved / Disbursed / Rejected / Closed` (DB). Computed states: Almost Due (2 days before due), Due, Arrears, Past Maturity, Defaulter (past maturity > 1 month), Missed Payment.
- Payment verification is manual (Manager/Director). M-Pesa is future scope. Locations = Google Maps links.
- **Application fee (2026-08-07):** collected at loan application, NOT registration. Tiers: 4–10k → KES 800 new / 600 existing; >10k → 1,500 new / 1,000 existing (existing = client with ≥1 disbursed/closed loan). Non-refundable, excluded from the repayment schedule. `POST /loans` refuses applications below KES 4,000 or without a **verified** `fee_payments` row matching the quote; the fee is consumed (linked) by the first loan. Recorder may verify their own record (cash collected at the desk, 2026-08-07). `fee_service.py` = quote/eligibility logic.

## Known bugs & current work (see PLAN.md)

1. ✅ **Fixed 2026-08-07** — permission mismatches `clients.edit`→`clients.update`, `repayments.create`→`repayments.record` (`loans_clients.py:288`, `:681`). Do not reintroduce.
2. ✅ **Fixed 2026-08-07** — duplicated admin route block deleted (`admin.py`); duplicate unguarded `GET /branches` removed from `loans_clients.py` (guarded branches.py version now serves).
3. ⚠️ **Decision reversed 2026-08-07** — `backend/.env` was committed by design, but GitHub push protection blocks any push containing the real Resend key, so it is now **gitignored** (history purged). Copy `.env.example` to `backend/.env` locally. Treat the exposed keys as compromised: **rotate the Resend API key and SECRET_KEY**, never add secrets to any other file.
4. ✅ **Fixed 2026-08-07** — permission resolution `ur.role.permissions` raised `AttributeError` (Role has no `permissions` relationship) → 500 on every permission-checked endpoint for authenticated users (notifications, loans, clients, repayments, reports, branches). All routers now use `get_user_permissions(db, user)` from `app/core/permissions.py`; `_require_permission` signature is `(db, user, perm)`.
5. ✅ **Fixed 2026-08-07** — notification read-state now DB-backed (`notification_reads` + `PATCH /notifications/{id}/read`); `5df95998dc2f` added `PENDING_APPROVAL` to the userstatus enum (invitees could not be inserted); `RESEND_FROM_EMAIL` override in `.env` (key is domain-restricted to `faraja.enkaai.net`).
6. Missing (planned): financial report, APScheduler email jobs, PDF service. ✅ `POST /auth/complete-profile` shipped 2026-08-07 (see invite flow note below).
6b. ✅ **Invite flow (2026-08-07)** — `accept-invite` creates the user (`PENDING_APPROVAL`) but the invite stays `PENDING` until `POST /auth/complete-profile` saves phone/ID/photo (`users.phone`, `users.id_no`, `users.profile_photo` — migration `6f2942b8aaea`) and marks it `ACCEPTED`. Re-submitting accept-invite mid-setup is idempotent (returns the existing user). User can't log in until the Director approves (`PATCH /admin/users/{id}/approve` → `ACTIVE`). Frontend: accept-invite page = password step → profile step → success.
7. ✅ **Fixed 2026-08-07** — client registration 500 `AttributeError: 'dict' object has no attribute 'model_dump'`: Pydantic v2 `model_dump()` already serializes nested models to dicts, so `create_client`'s list comprehension crashed on any non-empty dependants/next-of-kin/properties (empty lists passed silently). Fixed: `**request.model_dump()` (`loans_clients.py`).
8. ✅ **Fixed 2026-08-07** — expired/invalid JWT returned 500 instead of 401 on every authed endpoint: `get_current_user` only caught `AuthenticationError` but jose raises `ValueError`. Fixed: catch both → 401 (`dependencies/auth.py`), so the frontend interceptor redirects to `/login?expired=true`.
9. ✅ **Fixed 2026-08-17 (calculation/calendar bug-fix pass, verified live)** — five findings from a code review, all in the backend:
   - **Installment rounding residual:** `generate_installment_schedule` gave every installment the same 2dp amount, so for uneven divisions (e.g. KES 13,000 / 3 → 4,333.33 × 3 = 12,999.99) a sub-cent residual stayed outstanding and the loan could never be Closed. The **last installment now absorbs the difference** so installments always sum exactly to `total_repayable`.
   - **Penalty cap enforced:** `calculate_penalty(outstanding, due_date, rate, interval, max_penalty_amount=None)` now caps the penalty; all 3 callers (`loans_clients._enrich_loan`, `reports.portfolio/arrears/pnl`) pass `loan_product.max_penalty_amount`. Previously the C6 admin-editable cap was stored but never applied.
   - **Nairobi day boundaries everywhere:** only the calendar endpoint used `today_nairobi()`; penalty/status/notifications/reports/P&L month bounds all used UTC dates, so between 00:00–03:00 EAT "Due Today"/"Almost Due"/"Arrears"/penalty/`days_overdue` ran one calendar day late and loans disbursed 21:00–24:00 UTC showed their installments one day early on the calendar. `app/core/time.py` now provides `today_nairobi()`, `as_nairobi_date(dt)` (UTC → Nairobi date, naive = UTC) and `utc_instant(d)` (Nairobi midnight → UTC instant for DB windows); all the above now use them.
   - **Notifications skip repaid loans:** due-today/tomorrow/almost-due/arrears sections skip loans with `get_outstanding <= 0` (fully repaid but not yet Closed previously produced daily "Loan in Arrears" noise); `due_today` description now shows the real outstanding, not `total_repayable`.
   - **Auto-numbering is max-based:** `_next_loan_number`/`_next_client_number` were `count+1` against **unique** columns — any deletion caused a duplicate-number IntegrityError 500 on the next application. Now they take `max(...) + 1` for the current (Nairobi) year.
10. ✅ **Fixed 2026-08-17 — loan list/detail branch-scope holes (verified live):** `GET /loans?branch_id=` used the explicit param WITHOUT checking scope (any `loans.view` holder could read any branch's loans) and `GET /loans/{id}` had no scope check at all. Both now go through `_resolve_branch_filter`/`_assert_branch_visible` (403 for scoped users out of branch; Directors/System Admins unchanged). Do not reintroduce raw `stmt.where(Loan.branch_id == branch_id)` before scope resolution in `get_loans`. **Write-side pass (same day, verified live):** loan approve/reject/disburse/close/notes/status, repayment record/verify, fee record/verify/quote, and expense verify now all call `_assert_branch_visible` — a Kilifi-scoped Manager/LO gets 403 on Mombasa records; before, any holder of the write permission could act on any branch's loans/repayments/fees/expenses.

## Staging database policy (2026-08-07) — FIRST AND LAST RESET

- **The staging DB was reset exactly once** (2026-08-07) so the seed could be rebuilt to match the current schema: 11 branches, 6 roles + 2 extra demo users (FS-LO002/FS-MGR002, Kilifi), 3 loan products, 9 demo clients (full KYC), 8 loans covering **every state** (Pending, Approved, Rejected, Performing, Arrears, Past Maturity, Defaulter, Paid/Closed), installments + repayments (verified **and** unverified for the Finance Officer), and 9 fee payments (8 verified + 1 unverified for the verification workflow). Seed data lives in `app/db/seed_data.py`; dates are day-offsets so states stay valid whenever reseeded.
- **This is the LAST full database reset. From now on: migrations only.** Any schema or data change ships as a new Alembic revision (`uv run alembic revision --autogenerate`, review it, apply with `uv run alembic upgrade head`). Never drop/recreate the staging or production database again without explicit user authorization.
- **RE-PROVISIONED 2026-08-09 (user-authorized):** the old staging Neon DB had a connection failure; the owner created a new Neon instance (`ep-winter-waterfall-…neon.tech/neondb`) and switched `backend/.env` to it. Fresh DB was built with `uv run alembic upgrade head` + `python -m app.db.seed` — same seed counts as above (verified live: 8 users, 9 clients, 8 loans, 3 products). ⚠️ **Active staging DB changed again (found 2026-08-17):** `backend/.env` now points at `ep-divine-feather-axofwdxd…neon.tech/neondb` (the owner's newer instance; `ep-winter-waterfall` is stale in these docs). Re-seeded 2026-08-17 with the current seed (adds FO `loans.view`). Confirm the live URL before any migration work — the Render service env var should match `backend/.env`. The local docker Postgres URL line still exists in `.env` (commented) but the Neon line is active (last wins; config validator rewrites `postgresql://` → `postgresql+psycopg://`). Never commit this `.env`.
- **⚠️ Dual-DB reality (2026-08-17):** `backend/.env` line 12 now has the **local docker Postgres (`localhost:5434`)** active and the Neon line 13 commented out — the owner runs the local DB for dev. `uv run alembic upgrade head` (and the dev server) therefore hits **local docker**, while Render/staging serves **Neon**. Migrations must be applied to **both**: run once normally (local), then once with `DATABASE_URL=<neon-url> uv run alembic upgrade head` (env var wins over the `.env` line). Verify with `alembic current` on each. Both DBs must be at the same head before declaring a migration done.
- `python -m app.db.seed` (and `POST /internal/seed` with `X-Seed-Key`) is **additive and idempotent** — safe to re-run any time; it never deletes or rewrites existing rows. ⚠️ **Additive-only gotcha (2026-08-17):** grants that must be REMOVED (e.g. the temporary Manager → `branches.view_all` row created before the exclusion was added to `ROLE_PERMISSIONS`) are NOT deleted by reseeding — delete them manually on both DBs (or add a migration).
- Staging DB connection details live in the Render service env vars; the active `backend/.env` now points at the new Neon staging DB (as of 2026-08-09).

## Boundaries — do not touch without asking

- `backend/.env` (secrets — gitignored, never commit), `frontend/.env.local`, `node_modules/`, `.next/`, `.venv/`, `uv.lock`/`pnpm-lock.yaml` (no dependency changes without approval), `alembic/versions/*` (migrations are immutable once applied — new revisions only), `PLAN.md` status sections (update only as part of a plan-update task).
- Never commit anything (no `git commit`/`git push` unless explicitly asked). **Exception:** roadmap-item completion commits are pre-approved by the owner (see "Roadmap execution" above) — code + `PLAN.md` + `AGENTS.md` in one Conventional Commit per item, pushed immediately.
- Never delete or rewrite seeded data definitions (`app/core/branches.py`, `app/core/permissions.py`, `app/db/seed.py`) without flagging that DB reseeding will be needed.

## Git workflow

If asked to commit: stage only files relevant to the task (check `git status` + `git diff` first), Conventional Commits style (`fix:`, `feat:`, `docs:`, `chore:`), one logical change per commit. No force push, no amending pushed commits. Roadmap items bypass the "ask first" rule — see "Roadmap execution".

## Prompting the user's way (for agents and humans)

Well-formed tasks for this repo look like:
> Inspect the relevant code, propose a plan, wait for approval, then implement the smallest safe change. Verify with `pnpm build` (frontend) or `ruff check` (backend) and explain what changed.

---
*Keep this file updated: when the architecture, permissions, or known-bugs change, update the matching section here and in `PLAN.md`.*
