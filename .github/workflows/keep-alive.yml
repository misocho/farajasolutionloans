name: keep-alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Faraja staging API
        run: curl -sf --max-time 20 https://farajasolutionloans.onrender.com/api/v1/health || echo "ping failed (non-fatal)"